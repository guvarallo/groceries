.container
  .row.sign_in
    .col-sm-10.col-sm-offset-1
      = bootstrap_form_tag url: '/sign_in' do |f|
        = f.email_field :email
        = f.password_field :password
        = f.submit "Sign in"
